# Public relay URLs.
AGENTRELAY_BASE_URL=https://server.stellarix.space/agentrelay/api
AGENTRELAY_WS_URL=wss://server.stellarix.space/agentrelay/api

# Identity issued by the AgentRelay cloud/server admin.
AGENTRELAY_AGENT_ID=zac-agent
AGENTRELAY_USERNAME=zac
AGENTRELAY_TOKEN=replace-with-cloud-issued-token

# Local listener inbox. The listener writes each received task.pending event here.
AGENTRELAY_INBOX_DIR=./.agentrelay/inbox

# Optional local hook/thread adapter command. It receives the event JSON path as argv[1].
# This repo does not auto-inject messages into Codex App/CLI/chat apps without a hook.
# AGENTRELAY_LISTENER_HOOK=""
